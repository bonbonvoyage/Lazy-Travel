// ============================================================
// Program.cs 需要加入的部分(Area 路由)
// 說明:找到專案根目錄的 Program.cs,在 app.MapControllerRoute(
// name: "default", ...) 那行「上面」加入這段,順序很重要,
// Area 路由要排在 default 路由之前。
// ============================================================

app.MapControllerRoute(
    name: "areas",
    pattern: "{area:exists}/{controller=Dashboard}/{action=Index}/{id?}");

// 加完後整段看起來應該像這樣:
//
// app.MapControllerRoute(
//     name: "areas",
//     pattern: "{area:exists}/{controller=Dashboard}/{action=Index}/{id?}");
//
// app.MapControllerRoute(
//     name: "default",
//     pattern: "{controller=Home}/{action=Index}/{id?}");
//
// app.Run();
