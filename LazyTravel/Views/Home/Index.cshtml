@{
    ViewData["Title"] = "首頁";
}

<div class="container py-5 text-center">
    <h1 class="mb-3">Lazy Travel</h1>
    <p class="text-secondary mb-4">前台頁面由負責組員開發(期末部分...行有餘可以一起做。</p>
    <div class="d-flex justify-content-center gap-2 flex-wrap">
        <a class="btn btn-coral" asp-controller="Explore" asp-action="Index">找行程</a>
        <a class="btn btn-outline-primary" asp-area="Admin" asp-controller="Dashboard" asp-action="Index">進入後台管理</a>
    </div>
</div>
