# 揪團找旅伴｜行程詳情頁 規格書

依據使用者提供的參考設計稿整理，對應目前 `tours-api` 的實作（[TravelGroupsController.cs](tours-api/Controllers/TravelGroupsController.cs)）。

## 1. 頁面用途

顯示單一揪團（TravelGroup）的完整資訊，讓訪客瀏覽行程內容，並可申請加入 / 退出揪團。

- 路由：`GET /TravelGroups/Details/{id}`（頁面外殼）
- 資料：`GET /TravelGroups/Data/{id}`（JSON，前端 fetch 後渲染）

## 2. 畫面區塊與資料來源

| 區塊 | 顯示內容 | 資料庫來源 | 備註 |
|---|---|---|---|
| 相簿 | 1 張主圖 + 3 張縮圖，第 4 張疊「更多圖片 +N」 | `TravelGroupImages`（`IsCover` 優先、`SortOrder` 排序） | 無照片時顯示空狀態文字 |
| 標題列 | 行程名稱、審核狀態標籤 | `TravelGroups.GroupTitle`、`ReviewStatus` | 狀態文字：正常／檢舉審核中／違規 |
| 簡介 | 一段文字說明 | `TravelGroups.Description` | |
| 行動按鈕 | 申請加入 / 退出揪團 / 私訊團主 / （團主看到的提示） | 見第 4 節 | 依訪客與此團的關係動態切換 |
| 行程表 | 依「第 N 天」分組，每天列出時段＋標題＋地點＋說明 | `TravelGroupItineraryItems`（依 `DayNumber`、`SortOrder`） | |
| 行程資訊卡 | 國家、地區、行程區間（含天數夜數）、人數上限／最少成行／目前參加 | `TravelGroups`（Country/Region/StartDate/EndDate/MinPeople/MaxPeople/CurrentPeople） | |
| 已加入的旅伴 | 團員大頭貼＋名字，團主標示皇冠與「團主」字樣 | `GroupMembers`（`IsRemoved = false`）＋`Members` | 目前人數 = 真實 `GroupMembers` 筆數，不是 `TravelGroups.CurrentPeople` 欄位 |
| 預算試算 | 每個項目名稱＋金額，總計「預估每人」 | `TravelGroupBudgets` | 目前類別皆為固定示範項目：住宿／交通／餐飲／門票與活動 |

### 未列入本次範圍

- 參考稿中的彩色標籤（例如「小資」「美食」「攝影」「悠閒慢走」）：資料庫目前沒有對應的欄位或關聯表，**沒有做**，避免又變成寫死的假資料。
- 「動態歷程」時間軸（例如「小旅人建立了揪團房間」）：需要額外設計 log 顯示邏輯，本次先不做。

## 3. 訪客身分（暫時方案）

`tours-api` 目前**沒有會員登入系統**。為了讓「申請加入 / 退出揪團」有真實資料可以寫，暫時比照「找行程」頁按讚/收藏的做法：

- 瀏覽器 Cookie（`ltvmid`）記住「這個瀏覽器對應資料庫裡哪一個既有會員」。
- 第一次造訪時，從既有會員中挑一個（排除官方帳號、盡量避開該團團主）綁定給這個 Cookie。
- 這個機制**不是真的登入**，只是讓按鈕背後有真實資料可以動，日後接上真正的會員登入系統後，這段應該整個換掉。

## 4. 行動按鈕邏輯

| 訪客身分 | 顯示按鈕 | 對應動作 |
|---|---|---|
| 是團主本人 | 「你是這個揪團的團主」（不可點擊） | 無 |
| 已是團員 | 「退出揪團」＋「私訊團主（待開發）」 | `POST /TravelGroups/Leave/{id}` |
| 已送出申請、等待審核 | 「申請審核中」（不可點擊） | 無 |
| 其他訪客 | 「申請加入」＋「私訊團主（待開發）」 | `POST /TravelGroups/Join/{id}` |

- **申請加入**：新增一筆 `JoinRequests`（狀態＝待審核），**不會**立刻變成團員，需要團主審核（審核流程目前尚未實作）。
- **退出揪團**：把 `GroupMembers.IsRemoved` 設為 true、記錄 `LeftAt`，團主不能對自己執行退出。
- **私訊團主**：按鈕存在但停用，因為目前沒有任何站內訊息／私訊系統可以接。

## 5. 已知限制 / 後續待辦

1. 沒有會員登入系統 → 訪客身分是「借用」既有示範會員，不是真正個人化的登入。
2. 沒有私訊系統 → 「私訊團主」純顯示，無法點擊。
3. 沒有加入審核流程頁面 → `JoinRequests` 建立後目前沒有團主端可以核准/拒絕的介面。
4. 沒有收藏房間的資料表 → 若要做「收藏房間」，需要先決定要新增資料表還是沿用其他機制，本次未實作。
5. 版面排版（相簿排列比例、卡片位置、間距）僅依參考稿盡量還原，尚未經過逐格截圖比對確認。
